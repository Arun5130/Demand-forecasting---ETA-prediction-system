/** ETA-prediction page submitting delivery context to the active ETA model. */

import { api } from "./api.js";
import { mountPage } from "./app.js";
import { setApiIndicator } from "../components/navbar.js";
import { formatDuration, formatNumber, showToast } from "./utils.js";

mountPage({ title:"ETA Prediction", content:`<div class="page-heading"><div><h1>Customer delivery ETA</h1><p>Estimate delivery duration using current delivery context and predicted demand.</p></div></div><div class="prediction-layout"><article class="card"><div class="card-header"><h2 class="card-title">Delivery context</h2></div><div class="card-body"><form class="form-grid" id="eta-form"><div class="form-field"><label for="restaurant">Restaurant</label><input id="restaurant" required placeholder="Restaurant identifier"></div><div class="form-field"><label for="customer">Customer</label><input id="customer" required placeholder="Customer identifier"></div><div class="form-field"><label for="distance">Distance (km)</label><input id="distance" type="number" min="0.1" step="0.1" required></div><div class="form-field"><label for="weather">Weather</label><select id="weather"><option>clear</option><option>rain</option><option>fog</option><option>storm</option></select></div><div class="form-field"><label for="traffic">Traffic level</label><select id="traffic"><option value="low">Low</option><option value="medium" selected>Medium</option><option value="high">High</option></select></div><div class="form-field"><label for="vehicle">Vehicle type</label><select id="vehicle"><option value="bike">Bike</option><option value="scooter">Scooter</option><option value="car">Car</option></select></div><div class="form-field"><label for="hour">Order hour</label><input id="hour" type="number" min="0" max="23" required></div><button class="button" id="eta-submit" type="submit">Predict ETA</button></form></div></article><article class="card"><div class="card-header"><h2 class="card-title">Prediction</h2><span class="status-badge" id="eta-model">Awaiting request</span></div><div class="card-body" id="eta-result"><div class="empty-state"><strong>No ETA prediction yet</strong><span>Enter delivery context to estimate arrival time.</span></div></div></article></div>` });
document.getElementById("hour").value = new Date().getHours();
document.getElementById("eta-form").addEventListener("submit", async (event) => { event.preventDefault(); const button = document.getElementById("eta-submit"); button.disabled = true; button.textContent = "Predicting…"; try { const result = await api.eta({ restaurant_id:document.getElementById("restaurant").value, customer_id:document.getElementById("customer").value, distance_km:Number(document.getElementById("distance").value), weather:document.getElementById("weather").value, traffic_level:document.getElementById("traffic").value, vehicle_type:document.getElementById("vehicle").value, hour:Number(document.getElementById("hour").value) }); renderEta(result); setApiIndicator(true, "API connected"); showToast("ETA prediction generated.", "success"); } catch (error) { setApiIndicator(false, "API unavailable"); showToast(error.message); } finally { button.disabled = false; button.textContent = "Predict ETA"; } });
function renderEta(result) { const eta = result.eta_seconds ?? result.predicted_eta_seconds ?? result.eta; const demand = result.predicted_demand ?? result.demand_level; document.getElementById("eta-model").textContent = result.model_version || "Active model"; document.getElementById("eta-result").innerHTML = `<div class="result-hero"><div><span class="result-label">Predicted delivery ETA</span><strong class="result-value">${formatDuration(eta)}</strong></div><span class="status-badge healthy">${formatNumber(eta)} sec</span></div><div class="result-grid"><div class="result-item"><span>Distance</span><strong>${formatNumber(result.distance_km ?? document.getElementById("distance").value, 1)} km</strong></div><div class="result-item"><span>Weather</span><strong>${result.weather ?? document.getElementById("weather").value}</strong></div><div class="result-item"><span>Traffic level</span><strong>${result.traffic_level ?? document.getElementById("traffic").value}</strong></div><div class="result-item"><span>Demand level</span><strong>${formatNumber(demand)}</strong></div></div>`; }
